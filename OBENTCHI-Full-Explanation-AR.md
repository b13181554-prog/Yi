# 📚 الشرح الكامل والشامل لمشروع OBENTCHI Trading Bot

> **تنبيه**: هذا شرح مفصل جداً وطويل يغطي كل جانب من جوانب المشروع. مخصص لفهم عميق ودقيق لكل نقطة في المشروع.

---

## 📑 جدول المحتويات

1. [نظرة عامة عن المشروع](#1-نظرة-عامة-عن-المشروع)
2. [الفكرة والهدف الأساسي](#2-الفكرة-والهدف-الأساسي)
3. [البنية المعمارية الكاملة](#3-البنية-المعمارية-الكاملة)
4. [قاعدة البيانات MongoDB](#4-قاعدة-البيانات-mongodb)
5. [نظام الدفع والسحب](#5-نظام-الدفع-والسحب)
6. [أنواع التحليلات الفنية](#6-أنواع-التحليلات-الفنية)
7. [نظام الاشتراكات والمستويات](#7-نظام-الاشتراكات-والمستويات)
8. [نظام المحللين والإشارات](#8-نظام-المحللين-والإشارات)
9. [نظام الإحالة والأرباح](#9-نظام-الإحالة-والأرباح)
10. [نظام الأمان والحماية](#10-نظام-الأمان-والحماية)
11. [الذكاء الاصطناعي AI](#11-الذكاء-الاصطناعي-ai)
12. [الأوامر المتاحة في البوت](#12-الأوامر-المتاحة-في-البوت)
13. [تطبيق الويب Telegram Web App](#13-تطبيق-الويب-telegram-web-app)
14. [مصادر البيانات APIs](#14-مصادر-البيانات-apis)
15. [التقنيات المستخدمة](#15-التقنيات-المستخدمة)
16. [نظام الطوابير Queues](#16-نظام-الطوابير-queues)
17. [نظام التخزين المؤقت Caching](#17-نظام-التخزين-المؤقت-caching)
18. [نظام المراقبة Monitoring](#18-نظام-المراقبة-monitoring)
19. [الأنظمة المتقدمة](#19-الأنظمة-المتقدمة)
20. [النشر والبيئة](#20-النشر-والبيئة)

---

## 1. نظرة عامة عن المشروع

### 1.1 ما هو OBENTCHI؟

**OBENTCHI Trading Bot** (أوبينتشي) هو بوت تداول احترافي يعمل على منصة Telegram (تيليجرام). هذا البوت مصمم خصيصاً لمساعدة المتداولين العرب والأجانب في:

- **التحليل الفني للأسواق المالية** (عملات رقمية، فوركس، أسهم، سلع، مؤشرات)
- **الحصول على إشارات تداول ذكية** مدعومة بالذكاء الاصطناعي
- **إدارة المحافظ المالية** بعملة USDT (Tether) على شبكة TRON
- **متابعة أفضل المحللين** والاشتراك في خدماتهم
- **كسب المال من خلال الإحالات** ونظام العمولات

### 1.2 لماذا تم إنشاء هذا المشروع؟

تم إنشاء المشروع لحل عدة مشاكل يواجهها المتداولون:

1. **صعوبة التحليل الفني**: ليس كل شخص يعرف كيف يقرأ المؤشرات الفنية أو يحلل الرسوم البيانية
2. **ارتفاع تكاليف الاشتراكات**: الكثير من خدمات التحليل باهظة الثمن
3. **عدم التوافق مع الشريعة الإسلامية**: معظم منصات التداول تقدم خدمات محرمة مثل التداول بالرافعة المالية
4. **تعقيد الواجهات**: الكثير من المنصات معقدة وصعبة الاستخدام
5. **عدم الشفافية**: المتداولون لا يعرفون ما إذا كانت الإشارات حقيقية أم لا

### 1.3 ما الذي يجعل OBENTCHI مميزاً؟

- ✅ **متوافق مع الشريعة الإسلامية 100%**: لا يوجد تداول بالهامش أو عقود آجلة (Futures)
- ✅ **مجاني للبدء**: نسخة تجريبية مجانية لمدة 7 أيام
- ✅ **دعم 7 لغات**: العربية، الإنجليزية، الفرنسية، الإسبانية، الألمانية، الروسية، الصينية
- ✅ **ذكاء اصطناعي متقدم**: يستخدم Google Gemini AI لتحليل المشاعر والأخبار
- ✅ **سهل الاستخدام**: واجهة بسيطة داخل تيليجرام، لا حاجة لتطبيقات إضافية
- ✅ **شفاف وموثوق**: جميع البيانات حقيقية من مصادر موثوقة
- ✅ **دعم أسواق متعددة**: عملات رقمية، فوركس، أسهم، سلع، مؤشرات

---

## 2. الفكرة والهدف الأساسي

### 2.1 الفكرة الرئيسية

تخيل أن لديك خبير تداول شخصي متاح 24/7 يعمل على هاتفك في تطبيق تيليجرام. هذا الخبير:
- يحلل لك أي سوق تريده في ثوانٍ
- يعطيك توصيات واضحة: شراء، بيع، أو انتظار
- يحدد لك نقاط الدخول والخروج ووقف الخسارة
- يخبرك بمستوى الثقة في كل توصية
- يشرح لك الأسباب وراء كل قرار
- متاح بلغتك المفضلة

**هذا هو OBENTCHI بالضبط!**

### 2.2 الأهداف الاستراتيجية

#### الهدف قصير المدى (0-6 أشهر):
- الوصول إلى **10,000 مستخدم نشط**
- بناء قاعدة من **100 محلل موثوق**
- تحقيق **$5,000 إيرادات شهرية**
- إطلاق النسخة العربية والإنجليزية بالكامل

#### الهدف متوسط المدى (6-18 شهر):
- الوصول إلى **100,000 مستخدم نشط**
- بناء قاعدة من **1,000 محلل**
- تحقيق **$50,000 إيرادات شهرية**
- دعم جميع اللغات السبع بالكامل
- إضافة أسواق جديدة (معادن ثمينة، طاقة، عقارات)

#### الهدف طويل المدى (18+ شهر):
- الوصول إلى **1,000,000+ مستخدم نشط**
- أن نصبح المنصة رقم #1 للتداول المتوافق مع الشريعة في العالم العربي
- تحقيق **$500,000+ إيرادات شهرية**
- إطلاق تطبيقات موبايل مستقلة (iOS & Android)
- بناء أكاديمية تعليمية للمتداولين

### 2.3 الفئة المستهدفة

#### المستخدمون الأساسيون:
1. **المتداولون المبتدئون**: الذين يريدون البدء في التداول ولكنهم لا يعرفون من أين يبدأون
2. **المتداولون المتوسطون**: الذين لديهم خبرة ولكنهم يريدون أدوات إضافية للتحليل
3. **المتداولون المحترفون**: الذين يبحثون عن أداة سريعة للتحليل المتعدد
4. **المستثمرون المسلمون**: الذين يريدون التداول بطريقة متوافقة مع الشريعة
5. **المحللون الفنيون**: الذين يريدون منصة لبيع تحليلاتهم وإشاراتهم

#### التوزيع الجغرافي المتوقع:
- **50%** من الدول العربية (السعودية، الإمارات، مصر، الكويت، قطر)
- **30%** من دول آسيا (إندونيسيا، ماليزيا، تركيا)
- **15%** من أوروبا وأمريكا (المغتربون العرب)
- **5%** من باقي العالم

---

## 3. البنية المعمارية الكاملة

### 3.1 نظرة عامة على البنية

المشروع يستخدم **بنية الخدمات المصغرة (Microservices Architecture)**، وهذا يعني أن كل جزء من النظام يعمل بشكل مستقل ويمكن توسيعه أو تحديثه دون التأثير على باقي الأجزاء.

```
┌─────────────────────────────────────────────────────────────┐
│                    OBENTCHI Trading Bot                      │
│                    Complete Architecture                     │
└─────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          │                   │                   │
    ┌─────▼─────┐      ┌─────▼─────┐      ┌─────▼─────┐
    │  Frontend  │      │  Backend  │      │ Database  │
    │  (Client)  │      │  (Server) │      │  (Storage)│
    └────────────┘      └───────────┘      └───────────┘
```

### 3.2 الطبقة الأمامية (Frontend Layer)

#### 3.2.1 Telegram Bot Interface

هذا هو **الوجه الأول** الذي يراه المستخدم. عندما يفتح المستخدم البوت في تيليجرام، يرى:

- **رسالة ترحيب**: تشرح ما يفعله البوت
- **زر "🚀 فتح التطبيق"**: يفتح تطبيق الويب المدمج
- **أوامر نصية**: مثل `/start`, `/help`, `/analyze`, إلخ
- **أزرار تفاعلية**: للتنقل السريع بين الوظائف

**الملفات المسؤولة**:
- `bot.js`: يحتوي على جميع أوامر البوت والردود
- `bot-webhook.js`: يستقبل الرسائل من Telegram عبر Webhooks

#### 3.2.2 Telegram Web App

هذا هو **التطبيق الذكي** الذي يفتح داخل تيليجرام عندما تضغط على "فتح التطبيق". يحتوي على:

**الصفحات (Pages)**:
1. **index.html**: الصفحة الرئيسية والتحليل الفني
2. **dashboard.html**: لوحة التحكم الشخصية
3. **how-to-use.html**: دليل الاستخدام
4. **privacy.html**: سياسة الخصوصية
5. **admin-dashboard.html**: لوحة المدير (للمالك فقط)
6. **code-agent.html**: المساعد البرمجي الذكي (للمالك فقط)

**التصميم**:
- **نظام ألوان داكن**: خلفية سوداء/رمادية داكنة مريحة للعين
- **تدرجات لونية**: تدرجات بنفسجية وزرقاء جذابة
- **استجابة كاملة (Responsive)**: يعمل على جميع أحجام الشاشات
- **سلس وسريع**: انتقالات سلسة بين الصفحات

**الملفات**:
- `public/index.html`: الصفحة الرئيسية
- `public/css/style.css`: ملف التنسيقات
- `public/js/app.js`: المنطق الأساسي لتطبيق الويب (14,000+ سطر!)
- `public/js/translations.js`: الترجمات للغات السبع

### 3.3 الطبقة الخلفية (Backend Layer)

#### 3.3.1 الخادم الموحد (Unified Webhook Server)

هذا هو **قلب النظام**. ملف واحد يجمع كل الخدمات:

**الملف**: `services/unified-webhook-server.js`

**المسؤوليات**:
1. **استقبال Webhooks من Telegram**: عندما يرسل مستخدم رسالة
2. **خدمة HTTP API**: لتطبيق الويب
3. **معالجة الطوابير (Queue Processing)**: للمهام الثقيلة
4. **المجدولات (Schedulers)**: للمهام الدورية
5. **خدمة الملفات الثابتة**: للصور و CSS و JS

**لماذا خادم موحد؟**
- **أبسط في النشر**: ملف واحد بدلاً من 4-5 خدمات منفصلة
- **أقل استهلاكاً للموارد**: عملية واحدة بدلاً من عدة عمليات
- **مثالي لـ Replit**: يعمل بشكل مثالي على منصة Replit

**كيف يعمل؟**
```javascript
// عند تشغيل الخادم:
1. الاتصال بقاعدة البيانات MongoDB
2. الاتصال بـ Redis للطوابير والتخزين المؤقت
3. إعداد Telegram Webhook
4. تشغيل Express Server على المنفذ 5000
5. بدء معالجات الطوابير
6. بدء المجدولات
7. جاهز لاستقبال الطلبات!
```

#### 3.3.2 نظام Webhook vs Polling

**ما هو الفرق؟**

**Polling (الطريقة القديمة)**:
- البوت يسأل Telegram كل ثانية: "هل هناك رسائل جديدة؟"
- مثل شخص يفتح صندوق البريد كل دقيقة ليتحقق من وجود رسائل
- **مشاكل**: بطيء، يستهلك موارد، لا يمكن تشغيل نسختين معاً

**Webhook (الطريقة الحالية)**:
- Telegram يرسل الرسالة مباشرة للخادم فوراً
- مثل جرس الباب: يرن فوراً عند وصول شخص
- **مزايا**: سريع جداً، لا يستهلك موارد، يمكن تشغيل نسخ متعددة

**الكود في المشروع**:
```javascript
// في unified-webhook-server.js
bot.setWebHook(`${PUBLIC_URL}/webhook/${BOT_TOKEN}`);
```

### 3.4 طبقة البيانات (Data Layer)

#### 3.4.1 قاعدة البيانات الأساسية (MongoDB)

**لماذا MongoDB؟**
- **مرنة**: يمكن تغيير هيكل البيانات بسهولة
- **سريعة**: ممتازة للقراءة والكتابة السريعة
- **قابلة للتوسع**: يمكنها التعامل مع ملايين السجلات
- **دعم المعاملات**: لضمان سلامة البيانات المالية

**الاتصال**:
```
mongodb+srv://[username]:[password]@[cluster]/obentchi_bot
```

#### 3.4.2 التخزين المؤقت (Redis)

**لماذا Redis؟**
- **سريع جداً**: يخزن البيانات في الذاكرة (RAM)
- **موزع**: يمكن مشاركة البيانات بين عدة خوادم
- **متعدد الاستخدامات**: تخزين مؤقت + طوابير + جلسات

**الاستخدامات في المشروع**:
1. **تخزين الأسعار**: لمدة 30 ثانية لتقليل طلبات API
2. **تخزين بيانات المستخدمين**: لمدة 60 ثانية
3. **طوابير المهام**: للسحوبات والدفعات
4. **تحديد المعدل (Rate Limiting)**: لمنع الإساءة

### 3.5 التدفق الكامل للطلب (Request Flow)

دعنا نتتبع ما يحدث عندما يطلب مستخدم تحليلاً لعملة Bitcoin:

```
المستخدم
  │
  │ 1. يفتح تطبيق الويب
  ▼
Telegram Web App
  │
  │ 2. يختار BTC/USDT ويضغط "تحليل"
  ▼
API Request إلى الخادم
  │
  │ 3. POST /api/analysis
  ▼
Express Server (unified-webhook-server.js)
  │
  │ 4. التحقق من هوية المستخدم (Telegram WebApp Data)
  ▼
api-security.js
  │
  │ 5. التحقق من صحة البيانات ✅
  ▼
database.js
  │
  │ 6. جلب بيانات المستخدم من MongoDB
  ▼
user-access-control.js
  │
  │ 7. التحقق من حد الاستخدام (Rate Limit)
  ▼
market-data.js
  │
  │ 8. جلب سعر BTC من:
  │    - OKX API
  │    - Binance API (إذا فشل OKX)
  │    - CoinGecko API (إذا فشل Binance)
  ▼
intelligent-cache.js
  │
  │ 9. التحقق من التخزين المؤقت أولاً
  │    - إذا موجود: إرجاع فوراً ⚡
  │    - إذا لا: جلب من API وحفظ
  ▼
analysis.js (أو ultra-analysis.js أو v1-pro-analysis.js)
  │
  │ 10. تحليل فني شامل:
  │     - حساب 20+ مؤشر فني
  │     - تحديد الاتجاه
  │     - حساب نقاط الدخول/الخروج
  │     - تقييم المخاطر
  ▼
gemini-service.js (إذا كان AI مفعل)
  │
  │ 11. تحليل ذكي إضافي:
  │     - تحليل المشاعر
  │     - تحليل الأخبار
  │     - توصيات ذكية
  ▼
Response JSON إلى المستخدم
  │
  │ 12. {
  │      "success": true,
  │      "recommendation": "شراء قوي",
  │      "confidence": 85%,
  │      "entry_points": [...],
  │      ...
  │     }
  ▼
Web App
  │
  │ 13. عرض النتائج بشكل جميل
  ▼
المستخدم يرى النتيجة! 🎉
```

**الزمن الكلي**: عادة 2-5 ثوانٍ فقط!

---

## 4. قاعدة البيانات MongoDB

### 4.1 نظرة عامة

قاعدة البيانات هي **ذاكرة المشروع**. كل شيء يُحفظ فيها:
- من أنت؟ (بيانات المستخدمين)
- ماذا اشتريت؟ (الاشتراكات)
- كم معك من المال؟ (الأرصدة)
- من أحضرت للمنصة؟ (الإحالات)
- وأكثر بكثير...

### 4.2 المجموعات (Collections)

في MongoDB، البيانات تُخزن في "مجموعات" (Collections). كل مجموعة تشبه جدول في قاعدة بيانات تقليدية.

#### 4.2.1 مجموعة `users` (المستخدمون)

هذه هي **أهم مجموعة** في المشروع. تحتوي على معلومات كل مستخدم.

**الحقول الأساسية**:
```javascript
{
  user_id: 123456789,              // معرف التيليجرام الفريد
  username: "ahmad_trader",         // اسم المستخدم
  first_name: "أحمد",              // الاسم الأول
  last_name: "محمد",                // الاسم الأخير
  created_at: ISODate("2025-01-15"), // تاريخ التسجيل
  
  // معلومات الاشتراك
  free_trial_start: ISODate("2025-01-15"), // بداية الفترة التجريبية
  free_trial_used: false,           // هل استخدم الفترة التجريبية؟
  subscription_expires: null,       // تاريخ انتهاء الاشتراك
  subscription_tier: "free",        // المستوى: free, basic, vip, analyst, admin
  
  // المحفظة
  balance: 0,                       // الرصيد بالـ USDT
  total_deposits: 0,                // إجمالي الإيداعات
  total_withdrawals: 0,             // إجمالي السحوبات
  
  // الإحالات
  referred_by: null,                // من قام بإحالته؟
  referred_by_analyst: null,        // محلل قام بإحالته؟
  referral_earnings: 0,             // أرباحه من الإحالات
  total_referrals: 0,               // عدد من أحضرهم
  
  // الإعدادات
  language: "ar",                   // اللغة المفضلة
  selected_symbol: "BTCUSDT",       // العملة المفضلة
  selected_timeframe: "1h",         // الإطار الزمني المفضل
  market_type: "crypto",            // نوع السوق المفضل
  
  // الإشعارات
  notifications_enabled: true,      // هل يريد إشعارات؟
  notification_markets: [           // الأسواق التي يريد متابعتها
    "crypto", 
    "forex", 
    "stocks"
  ],
  
  // معلومات المحلل (إذا كان محلل)
  is_analyst: false,                // هل هو محلل؟
  analyst_profile: {
    bio: "",                        // نبذة عنه
    success_rate: 0,                // نسبة النجاح
    total_signals: 0,               // عدد الإشارات
    subscribers_count: 0,           // عدد المشتركين
    monthly_price: 50,              // سعر الاشتراك الشهري
    commission_rate: 20             // عمولته %
  },
  
  // حالة الحساب
  is_active: true,                  // الحساب نشط؟
  is_banned: false,                 // محظور؟
  ban_reason: null,                 // سبب الحظر
  last_seen: ISODate("2025-01-15") // آخر ظهور
}
```

**الفهارس (Indexes)** للبحث السريع:
- `user_id`: فهرس فريد (unique index)
- `username`: فهرس عادي
- `subscription_expires`: لإيجاد الاشتراكات المنتهية
- `is_analyst + is_active`: لإيجاد المحللين النشطين
- `referred_by`: لحساب الإحالات

**مثال عملي**:
```javascript
// البحث عن مستخدم
const user = await db.collection('users').findOne({ user_id: 123456789 });

// تحديث رصيد مستخدم
await db.collection('users').updateOne(
  { user_id: 123456789 },
  { $inc: { balance: 100 } }  // زيادة 100 USDT
);

// إيجاد جميع المحللين النشطين
const analysts = await db.collection('users').find({
  is_analyst: true,
  is_active: true
}).toArray();
```

#### 4.2.2 مجموعة `transactions` (المعاملات المالية)

كل عملية مالية (إيداع، سحب، دفع اشتراك، عمولة) تُسجل هنا.

**الحقول**:
```javascript
{
  _id: ObjectId("..."),             // المعرف الفريد
  user_id: 123456789,               // معرف المستخدم
  type: "deposit",                  // النوع: deposit, withdrawal, subscription, commission
  amount: 100,                      // المبلغ بالـ USDT
  status: "completed",              // الحالة: pending, completed, failed
  
  // تفاصيل إضافية
  transaction_hash: "0x123...",     // hash المعاملة على البلوكشين
  wallet_address: "TXn7...",        // عنوان المحفظة
  description: "إيداع USDT",        // وصف
  
  // معلومات النظام
  created_at: ISODate("2025-01-15"),
  updated_at: ISODate("2025-01-15"),
  
  // بيانات إضافية (اختيارية)
  metadata: {
    source: "cryptapi",             // مصدر المعاملة
    fee: 1,                         // الرسوم
    net_amount: 99                  // المبلغ بعد الرسوم
  }
}
```

**أنواع المعاملات**:
1. **deposit**: إيداع USDT في المحفظة
2. **withdrawal**: سحب USDT من المحفظة
3. **subscription**: دفع اشتراك
4. **commission**: عمولة من إحالة
5. **refund**: استرداد مبلغ
6. **analyst_payment**: دفع لمحلل
7. **bonus**: مكافأة

**الفهارس**:
- `user_id + created_at`: لعرض تاريخ معاملات المستخدم
- `user_id + status`: لإيجاد المعاملات المعلقة
- `type + status`: لإحصائيات النظام
- `transaction_hash`: للبحث بالـ hash

#### 4.2.3 مجموعة `withdrawal_requests` (طلبات السحب)

عندما يطلب مستخدم سحب أمواله، يُسجل الطلب هنا.

**الحقول**:
```javascript
{
  _id: ObjectId("..."),
  user_id: 123456789,
  amount: 100,                      // المبلغ المطلوب
  wallet_address: "TXn7...",        // عنوان محفظة المستخدم
  network: "TRC20",                 // الشبكة
  
  status: "pending",                // pending, processing, completed, failed
  
  // تفاصيل المعالجة
  processed_at: null,               // متى تمت المعالجة؟
  withdraw_id: null,                // معرف السحب من OKX
  transaction_hash: null,           // hash المعاملة
  
  // معلومات الفشل (إذا فشل)
  failure_reason: null,             // سبب الفشل
  retry_count: 0,                   // عدد المحاولات
  
  created_at: ISODate("2025-01-15"),
  updated_at: ISODate("2025-01-15")
}
```

**دورة حياة طلب السحب**:
```
1. المستخدم يطلب سحب 100 USDT
   ↓ status = "pending"
   
2. النظام يتحقق من الرصيد
   ↓ هل الرصيد كافٍ؟
   
3. إذا نعم: يُضاف إلى طابور السحوبات
   ↓ status = "processing"
   
4. معالج الطابور يرسل طلب إلى OKX API
   ↓ محاولة السحب...
   
5. إذا نجح: تحديث الحالة
   ↓ status = "completed"
   
6. إذا فشل: إعادة المحاولة (حتى 3 مرات)
   ↓ status = "failed" بعد المحاولة الأخيرة
   
7. إشعار المستخدم والمالك
   ✅ تم / ❌ فشل
```

#### 4.2.4 مجموعة `cryptapi_payments` (دفعات CryptAPI)

عندما يودع مستخدم USDT عبر CryptAPI، تُسجل المعاملة هنا.

**الحقول**:
```javascript
{
  _id: ObjectId("..."),
  user_id: 123456789,
  
  // معلومات الدفع
  payment_address: "TAbC...",       // عنوان الدفع الفريد
  amount_requested: 100,            // المبلغ المطلوب
  amount_received: 99.5,            // المبلغ المستلم فعلياً
  
  // حالة الدفع
  status: "pending",                // pending, confirmed, completed
  confirmations: 0,                 // عدد التأكيدات على البلوكشين
  
  // معلومات CryptAPI
  callback_data: {},                // البيانات الواردة من CryptAPI
  qr_code_url: "https://...",       // رابط QR Code
  
  // معلومات المعاملة
  txid: "0x123...",                 // معرف المعاملة
  fee_paid: 0.5,                    // الرسوم المدفوعة
  
  created_at: ISODate("2025-01-15"),
  confirmed_at: null,               // متى تم التأكيد؟
  completed_at: null                // متى اكتملت المعاملة؟
}
```

**كيف تعمل عملية الإيداع؟**:
```
1. المستخدم يضغط "إيداع"
   ↓
   
2. النظام يطلب عنوان دفع فريد من CryptAPI
   ↓ POST /api/cryptapi/create-payment
   
3. CryptAPI يُنشئ عنوان TRC20 فريد
   ↓ TAbC123xyz...
   
4. النظام يحفظ العنوان في قاعدة البيانات
   ↓ status = "pending"
   
5. المستخدم يرى العنوان + QR Code
   ↓ يرسل USDT من محفظته
   
6. CryptAPI يراقب العنوان تلقائياً
   ↓ كل 10 ثوانٍ
   
7. عند وصول USDT: CryptAPI يرسل callback
   ↓ POST /api/cryptapi/callback
   
8. النظام يتحقق من صحة الـ callback
   ↓ التحقق من التوقيع (RSA-SHA256)
   
9. إذا صحيح: تحديث الرصيد
   ↓ balance += amount_received
   ↓ status = "completed"
   
10. إشعار المستخدم
    ✅ تم إضافة 99.5 USDT إلى محفظتك!
```

#### 4.2.5 مجموعة `subscriptions` (الاشتراكات)

تتبع جميع اشتراكات المستخدمين (الأساسية والمحللين).

**الحقول**:
```javascript
{
  _id: ObjectId("..."),
  user_id: 123456789,               // المستخدم المشترك
  
  subscription_type: "basic",       // basic, vip, analyst
  
  // إذا كان اشتراك محلل
  analyst_id: null,                 // معرف المحلل (إذا كان اشتراك محلل)
  
  // التواريخ
  start_date: ISODate("2025-01-01"),
  end_date: ISODate("2025-02-01"),  // 30 يوم
  
  // الدفع
  amount_paid: 10,                  // المبلغ المدفوع
  payment_method: "wallet",         // wallet أو تجريبي
  
  // الحالة
  status: "active",                 // active, expired, cancelled
  auto_renew: false,                // تجديد تلقائي؟
  
  created_at: ISODate("2025-01-01"),
  updated_at: ISODate("2025-01-01")
}
```

#### 4.2.6 مجموعة `analyst_signals` (إشارات المحللين)

المحللون ينشرون إشاراتهم هنا.

**الحقول**:
```javascript
{
  _id: ObjectId("..."),
  analyst_id: 987654321,            // معرف المحلل
  
  // معلومات الإشارة
  symbol: "BTCUSDT",                // العملة/الزوج
  market_type: "crypto",            // نوع السوق
  timeframe: "1h",                  // الإطار الزمني
  
  // التوصية
  signal_type: "buy",               // buy, sell, hold
  entry_price: 50000,               // سعر الدخول
  
  // إدارة المخاطر
  take_profit_levels: [52000, 54000, 56000], // أهداف الربح
  stop_loss: 48000,                 // وقف الخسارة
  
  // التحليل
  analysis: "اتجاه صاعد قوي...",    // التحليل المكتوب
  confidence_level: 85,             // مستوى الثقة %
  risk_level: "medium",             // low, medium, high
  
  // النتيجة (بعد إغلاق الصفقة)
  result: null,                     // win, loss, breakeven
  actual_profit: null,              // الربح الفعلي %
  close_price: null,                // سعر الإغلاق
  closed_at: null,                  // متى أُغلقت؟
  
  // الإحصائيات
  views_count: 150,                 // عدد المشاهدات
  likes_count: 45,                  // عدد الإعجابات
  
  // الحالة
  status: "active",                 // active, closed, cancelled
  is_premium: true,                 // للمشتركين فقط؟
  
  created_at: ISODate("2025-01-15"),
  expires_at: ISODate("2025-01-16") // متى تنتهي صلاحيتها؟
}
```

#### 4.2.7 مجموعة `security_events` (أحداث الأمان)

تسجيل جميع الأحداث الأمنية المهمة.

**الحقول**:
```javascript
{
  _id: ObjectId("..."),
  user_id: 123456789,
  
  event_type: "suspicious_login",   // نوع الحدث
  severity: "medium",               // low, medium, high, critical
  
  description: "تسجيل دخول من IP جديد",
  
  // تفاصيل إضافية
  ip_address: "192.168.1.1",
  user_agent: "Telegram/...",
  location: "Saudi Arabia, Riyadh",
  
  // الإجراء المتخذ
  action_taken: "notification_sent", // none, notification_sent, account_frozen, etc.
  
  // البيانات الإضافية
  metadata: {
    previous_ip: "192.168.1.100",
    risk_score: 45
  },
  
  created_at: ISODate("2025-01-15")
}
```

**أنواع الأحداث الأمنية**:
- `suspicious_login`: تسجيل دخول مشبوه
- `rapid_withdrawals`: سحوبات سريعة متتالية
- `unusual_activity`: نشاط غير عادي
- `failed_authentication`: فشل في المصادقة
- `account_takeover_attempt`: محاولة اختراق
- `multiple_failed_transactions`: معاملات فاشلة متعددة

#### 4.2.8 مجموعة `feature_flags` (أعلام الميزات)

للتحكم في الميزات ديناميكياً دون إعادة تشغيل الخادم.

**الحقول**:
```javascript
{
  _id: ObjectId("..."),
  flag_name: "ultra_analysis",      // اسم الميزة
  
  enabled: true,                    // مفعلة؟
  
  // التحكم المتقدم
  rollout_percentage: 100,          // نسبة الطرح %
  
  // قيود الوصول
  allowed_tiers: ["vip", "analyst", "admin"], // المستويات المسموحة
  allowed_users: [],                // مستخدمون محددون
  blocked_users: [],                // مستخدمون محظورون
  
  // معلومات
  description: "تحليل فني فائق الدقة",
  created_by: 123456789,            // من أنشأها؟
  
  created_at: ISODate("2025-01-01"),
  updated_at: ISODate("2025-01-15")
}
```

**كيف تُستخدم؟**
```javascript
// التحقق من ميزة
const result = await featureFlagService.evaluateFlag('ultra_analysis', {
  userId: 123456789,
  tier: 'vip'
});

if (result.enabled) {
  // تنفيذ التحليل الفائق
} else {
  // عرض رسالة: هذه الميزة غير متاحة لك
}
```

#### 4.2.9 مجموعة `referrals` (الإحالات)

تتبع من أحضر من إلى المنصة.

**الحقول**:
```javascript
{
  _id: ObjectId("..."),
  
  referrer_id: 123456789,           // المُحيل (من دعا)
  referred_id: 987654321,           // المُحال (من انضم)
  
  // نوع الإحالة
  referral_type: "user",            // user, analyst
  analyst_id: null,                 // إذا كانت إحالة محلل
  
  // الحالة
  status: "active",                 // active, inactive
  conversion_status: "converted",   // pending, converted
  
  // الأرباح
  total_earned: 15.5,               // إجمالي الأرباح من هذه الإحالة
  
  // مستويات الإحالة
  level: 1,                         // 1, 2, 3 (إحالة متعددة المستويات)
  
  created_at: ISODate("2025-01-01")
}
```

**نظام الإحالة متعدد المستويات**:
```
أحمد (المستوى 0 - الأصل)
  │
  ├─> محمد (المستوى 1 - إحالة مباشرة)
  │     │
  │     └─> علي (المستوى 2 - إحالة من إحالة)
  │           │
  │           └─> خالد (المستوى 3 - إحالة من إحالة من إحالة)
  │
  └─> سارة (المستوى 1 - إحالة مباشرة)
```

**العمولات**:
- **المستوى 1**: 20% من مشتريات المُحال المباشر
- **المستوى 2**: 10% من مشتريات إحالات المُحال
- **المستوى 3**: 5% من مشتريات إحالات إحالات المُحال

#### 4.2.10 مجموعات أخرى

1. **`notifications_log`**: سجل جميع الإشعارات المرسلة
2. **`api_costs`**: تتبع تكاليف استدعاءات APIs الخارجية
3. **`rate_limits`**: تخزين حدود الاستخدام
4. **`cache_metadata`**: بيانات التخزين المؤقت
5. **`admin_actions`**: سجل إجراءات المدير
6. **`system_health`**: سجل صحة النظام
7. **`error_logs`**: سجل الأخطاء

### 4.3 الفهارس (Indexes)

الفهارس تجعل البحث سريعاً جداً. بدون فهارس، البحث عن مستخدم واحد من مليون مستخدم سيستغرق ثوانٍ. مع الفهارس، يستغرق أجزاء من الثانية!

**الفهارس المُنشأة**:
```javascript
// على مجموعة users
{ user_id: 1 }                      // فريد
{ username: 1 }
{ subscription_expires: 1 }
{ is_analyst: 1, is_active: 1 }
{ referred_by: 1 }

// على مجموعة transactions
{ user_id: 1, created_at: -1 }
{ user_id: 1, status: 1 }
{ type: 1, status: 1 }
{ transaction_hash: 1 }

// على مجموعة withdrawal_requests
{ user_id: 1, status: 1 }
{ status: 1, created_at: 1 }

// على مجموعة cryptapi_payments
{ user_id: 1, status: 1 }
{ payment_address: 1 }              // فريد
{ status: 1 }

// على مجموعة security_events
{ user_id: 1, created_at: -1 }
{ event_type: 1, created_at: -1 }

// على مجموعة feature_flags
{ flag_name: 1 }                    // فريد
{ enabled: 1 }
```

### 4.4 معاملات MongoDB (Transactions)

لضمان سلامة البيانات المالية، نستخدم **معاملات MongoDB**.

**مثال: دفع اشتراك**
```javascript
const session = client.startSession();

try {
  await session.withTransaction(async () => {
    // 1. خصم المبلغ من المحفظة
    await db.collection('users').updateOne(
      { user_id: 123456789 },
      { $inc: { balance: -10 } },
      { session }
    );
    
    // 2. إضافة اشتراك
    await db.collection('subscriptions').insertOne({
      user_id: 123456789,
      subscription_type: "basic",
      start_date: new Date(),
      end_date: new Date(Date.now() + 30*24*60*60*1000),
      amount_paid: 10,
      status: "active"
    }, { session });
    
    // 3. إضافة معاملة
    await db.collection('transactions').insertOne({
      user_id: 123456789,
      type: "subscription",
      amount: 10,
      status: "completed",
      created_at: new Date()
    }, { session });
    
    // إذا نجح كل شيء، يتم التأكيد (commit)
    // إذا فشل أي شيء، يتم التراجع (rollback) عن كل شيء
  });
} finally {
  await session.endSession();
}
```

**الفائدة**: إما تنجح العملية بالكامل، أو تفشل بالكامل. لا يمكن أن يُخصم المبلغ دون إضافة الاشتراك!

---

## 5. نظام الدفع والسحب

### 5.1 نظرة عامة

نظام الدفع والسحب هو **قلب النظام المالي** في المشروع. يجب أن يكون:
- **آمن 100%**: لا يمكن خسارة أموال المستخدمين
- **سريع**: معالجة فورية أو شبه فورية
- **موثوق**: يعمل 24/7 دون أعطال
- **شفاف**: المستخدم يرى كل شيء

### 5.2 نظام الإيداع (Deposits)

#### 5.2.1 التكامل مع CryptAPI

**ما هو CryptAPI؟**
- خدمة خارجية متخصصة في معالجة دفعات العملات الرقمية
- تُنشئ عناوين دفع فريدة لكل معاملة تلقائياً
- ترسل إشعارات فورية عند استلام الأموال
- **الرسوم**: 1% فقط من قيمة المعاملة

**الخطوات التفصيلية**:

**الخطوة 1: المستخدم يطلب إيداع**
```
المستخدم في Web App
  │
  │ يضغط "إيداع"
  ▼
يظهر نموذج: كم تريد أن تودع؟
  │
  │ يكتب: 100 USDT
  ▼
POST /api/cryptapi/create-payment
  │
  │ Body: {
  │   init_data: "...",  // بيانات Telegram
  │   amount: 100
  │ }
  ▼
```

**الخطوة 2: النظام يُنشئ عنوان دفع**
```javascript
// في ملف cryptapi.js

async createPayment(userId, amount) {
  // 1. التحقق من المبلغ
  if (amount < 1) {
    return { success: false, error: 'الحد الأدنى للإيداع 1 USDT' };
  }
  
  // 2. طلب عنوان من CryptAPI
  const callbackUrl = `${PUBLIC_URL}/api/cryptapi/callback`;
  const response = await axios.get(`https://api.cryptapi.io/trc20/usdt/create/`, {
    params: {
      callback: `${callbackUrl}?user_id=${userId}&amount=${amount}`,
      address: BOT_WALLET_ADDRESS,  // محفظتنا
      pending: 0,  // لا نقبل معاملات معلقة
      confirmations: 1  // نريد تأكيد واحد على الأقل
    }
  });
  
  // 3. حفظ في قاعدة البيانات
  await db.collection('cryptapi_payments').insertOne({
    user_id: userId,
    payment_address: response.data.address_in,
    amount_requested: amount,
    status: 'pending',
    qr_code_url: response.data.qrcode_url,
    created_at: new Date()
  });
  
  // 4. إرجاع العنوان للمستخدم
  return {
    success: true,
    data: {
      payment_address: response.data.address_in,
      qr_code_url: response.data.qrcode_url,
      amount: amount
    }
  };
}
```

**الخطوة 3: المستخدم يرى العنوان**
```
Web App يعرض:
┌──────────────────────────────────┐
│  💰 إيداع 100 USDT               │
├──────────────────────────────────┤
│  عنوان الدفع:                    │
│  TAbC123xyz... [نسخ]             │
│                                  │
│  [QR Code كبير]                  │
│                                  │
│  ⚠️ هام:                         │
│  • أرسل فقط USDT TRC20           │
│  • استخدم هذا العنوان مرة واحدة │
│  • الحد الأدنى: 1 USDT           │
│                                  │
│  ⏱ ينتهي بعد: 23:45             │
└──────────────────────────────────┘
```

**الخطوة 4: المستخدم يرسل USDT**
- يفتح محفظته (Trust Wallet, Binance, إلخ)
- يختار USDT على شبكة TRC20
- ينسخ العنوان أو يمسح QR Code
- يرسل 100 USDT
- ينتظر...

**الخطوة 5: CryptAPI يكتشف الدفعة**
```
CryptAPI (يراقب العنوان كل 10 ثوانٍ)
  │
  │ 🔍 وصل USDT!
  ▼
يرسل callback فوراً:
POST https://your-domain.com/api/cryptapi/callback?user_id=123&amount=100
  │
  │ Body: {
  │   "address_in": "TAbC123...",
  │   "address_out": "TXn7...",  // محفظتنا
  │   "txid_in": "0x123...",
  │   "txid_out": "0x456...",
  │   "value": 100,
  │   "value_coin": 100,
  │   "value_forwarded": 99,  // بعد رسوم 1%
  │   "confirmations": 1
  │ }
  │
  │ Header: {
  │   "x-ca-signature": "signature_here"
  │ }
  ▼
```

**الخطوة 6: النظام يتحقق ويُحدث الرصيد**
```javascript
// في ملف index.js - معالج callback

app.post('/api/cryptapi/callback', async (req, res) => {
  try {
    // 1. التحقق من التوقيع (Signature Verification)
    const signature = req.headers['x-ca-signature'];
    const isValid = await cryptapi.verifySignature(
      JSON.stringify(req.body),
      signature
    );
    
    if (!isValid) {
      logger.error('❌ Invalid signature from CryptAPI');
      return res.status(401).json({ error: 'Invalid signature' });
    }
    
    // 2. استخراج البيانات
    const { value_forwarded, txid_in, confirmations } = req.body;
    const userId = parseInt(req.query.user_id);
    const requestedAmount = parseFloat(req.query.amount);
    
    // 3. البحث عن الدفعة في قاعدة البيانات
    const payment = await db.collection('cryptapi_payments').findOne({
      user_id: userId,
      amount_requested: requestedAmount,
      status: 'pending'
    });
    
    if (!payment) {
      return res.status(404).json({ error: 'Payment not found' });
    }
    
    // 4. إضافة إلى طابور المعالجة
    await paymentCallbackQueue.add('process-payment', {
      userId: userId,
      amount: value_forwarded,
      paymentId: payment._id,
      txid: txid_in,
      confirmations: confirmations
    });
    
    // 5. الرد لـ CryptAPI (يجب أن نرد بسرعة!)
    res.status(200).json({ status: 'OK' });
    
  } catch (error) {
    logger.error('Error in callback:', error);
    res.status(500).json({ error: 'Internal server error' });
  }
});
```

**الخطوة 7: معالج الطابور يُنفذ**
```javascript
// في ملف payment-callback-queue.js

async function processPaymentCallback(job) {
  const { userId, amount, paymentId, txid, confirmations } = job.data;
  
  try {
    // استخدام معاملة MongoDB للأمان
    const session = client.startSession();
    
    await session.withTransaction(async () => {
      // 1. تحديث رصيد المستخدم
      await db.collection('users').updateOne(
        { user_id: userId },
        {
          $inc: {
            balance: amount,
            total_deposits: amount
          }
        },
        { session }
      );
      
      // 2. تحديث حالة الدفعة
      await db.collection('cryptapi_payments').updateOne(
        { _id: paymentId },
        {
          $set: {
            status: 'completed',
            amount_received: amount,
            txid: txid,
            confirmations: confirmations,
            completed_at: new Date()
          }
        },
        { session }
      );
      
      // 3. إضافة معاملة
      await db.collection('transactions').insertOne({
        user_id: userId,
        type: 'deposit',
        amount: amount,
        status: 'completed',
        transaction_hash: txid,
        description: `إيداع ${amount} USDT عبر CryptAPI`,
        created_at: new Date()
      }, { session });
    });
    
    await session.endSession();
    
    // 4. إشعار المستخدم
    await bot.sendMessage(userId, `
✅ <b>تم الإيداع بنجاح!</b>

💰 <b>المبلغ:</b> ${amount} USDT
🔗 <b>المعاملة:</b> <code>${txid}</code>
⏰ <b>الوقت:</b> ${new Date().toLocaleString('ar-SA')}

رصيدك الحالي: ${newBalance} USDT
    `, { parse_mode: 'HTML' });
    
    // 5. إشعار المالك
    await bot.sendMessage(OWNER_ID, `
🎉 <b>إيداع جديد!</b>

👤 المستخدم: ${userId}
💰 المبلغ: ${amount} USDT
🔗 المعاملة: ${txid}
    `, { parse_mode: 'HTML' });
    
    logger.info(`✅ Payment processed for user ${userId}: ${amount} USDT`);
    
  } catch (error) {
    logger.error(`❌ Payment processing failed:`, error);
    throw error;  // لإعادة المحاولة
  }
}
```

**الخطوة 8: المستخدم يرى الإشعار**
```
📱 إشعار Telegram:

✅ تم الإيداع بنجاح!

💰 المبلغ: 100 USDT
🔗 المعاملة: 0x123...
⏰ الوقت: ١٥ يناير ٢٠٢٥، ١٠:٣٠ ص

رصيدك الحالي: 100 USDT
```

#### 5.2.2 الأمان في نظام الإيداع

**1. التحقق من التوقيع (Signature Verification)**
- كل callback من CryptAPI يحتوي على توقيع RSA-SHA256
- نتحقق من صحة التوقيع قبل قبول الدفعة
- هذا يمنع أي شخص من إرسال callbacks مزيفة

**2. التحقق من المبالغ**
- نتحقق أن المبلغ المستلم يطابق المبلغ المطلوب
- إذا كان أقل، نرفض
- إذا كان أكثر، نقبل ونُضيف الفرق

**3. منع التكرار (Idempotency)**
- كل دفعة لها معرف فريد
- إذا حاول شخص إعادة إرسال نفس الـ callback، نرفضه
- نحفظ `txid` في قاعدة البيانات ونتحقق منه

**4. معاملات MongoDB**
- استخدام transactions لضمان سلامة البيانات
- إما تنجح العملية بالكامل أو تفشل بالكامل

**5. طابور المعالجة**
- المعالجة الفعلية تتم في طابور منفصل
- هذا يمنع تعطل الخادم تحت الضغط
- إذا فشلت المعالجة، تُعاد المحاولة تلقائياً

### 5.3 نظام السحب (Withdrawals)

#### 5.3.1 التكامل مع OKX API

**ما هو OKX؟**
- منصة تداول عملات رقمية عالمية
- نستخدم API الخاص بهم لإرسال USDT تلقائياً
- سريع وموثوق

**الخطوات التفصيلية**:

**الخطوة 1: المستخدم يطلب سحب**
```
المستخدم في Web App
  │
  │ يضغط "سحب"
  ▼
يظهر نموذج:
┌──────────────────────────────────┐
│  💸 طلب سحب                      │
├──────────────────────────────────┤
│  المبلغ: [100] USDT              │
│  الرصيد المتاح: 150 USDT         │
│                                  │
│  عنوان المحفظة (TRC20):          │
│  [TXn7...]                       │
│                                  │
│  ⚠️ رسوم السحب: 1 USDT           │
│  ستستلم: 99 USDT                 │
│                                  │
│  [تأكيد السحب]                   │
└──────────────────────────────────┘
```

**الخطوة 2: التحققات الأولية**
```javascript
// في ملف index.js - معالج طلب السحب

app.post('/api/withdraw', authenticateAPI, async (req, res) => {
  const { amount, wallet_address } = req.body;
  const userId = req.auth.user_id;
  
  try {
    // 1. التحقق من المبلغ
    if (amount < 5) {
      return res.json({
        success: false,
        error: 'الحد الأدنى للسحب 5 USDT'
      });
    }
    
    if (amount > 1000) {
      return res.json({
        success: false,
        error: 'الحد الأقصى للسحب 1000 USDT'
      });
    }
    
    // 2. التحقق من عنوان المحفظة
    if (!wallet_address.startsWith('T') || wallet_address.length !== 34) {
      return res.json({
        success: false,
        error: 'عنوان محفظة TRC20 غير صحيح'
      });
    }
    
    // 3. جلب بيانات المستخدم
    const user = await db.getUser(userId);
    
    // 4. التحقق من الرصيد
    const totalCost = amount + 1;  // المبلغ + الرسوم
    if (user.balance < totalCost) {
      return res.json({
        success: false,
        error: `رصيدك غير كافٍ. تحتاج ${totalCost} USDT`
      });
    }
    
    // 5. التحقق من عدم وجود طلب سحب معلق
    const pendingWithdrawal = await db.collection('withdrawal_requests').findOne({
      user_id: userId,
      status: { $in: ['pending', 'processing'] }
    });
    
    if (pendingWithdrawal) {
      return res.json({
        success: false,
        error: 'لديك طلب سحب معلق بالفعل'
      });
    }
    
    // ✅ كل شيء تمام! ننتقل للخطوة التالية...
```

**الخطوة 3: إنشاء طلب السحب**
```javascript
    // استخدام معاملة MongoDB
    const session = client.startSession();
    
    await session.withTransaction(async () => {
      // 1. خصم المبلغ من المحفظة
      await db.collection('users').updateOne(
        { user_id: userId },
        {
          $inc: {
            balance: -(amount + 1),  // المبلغ + الرسوم
            total_withdrawals: amount
          }
        },
        { session }
      );
      
      // 2. إنشاء طلب سحب
      const withdrawalRequest = await db.collection('withdrawal_requests').insertOne({
        user_id: userId,
        amount: amount,
        wallet_address: wallet_address,
        network: 'TRC20',
        status: 'pending',
        created_at: new Date()
      }, { session });
      
      // 3. إضافة معاملة
      await db.collection('transactions').insertOne({
        user_id: userId,
        type: 'withdrawal',
        amount: amount + 1,
        status: 'pending',
        description: `طلب سحب ${amount} USDT`,
        created_at: new Date()
      }, { session });
      
      // 4. إضافة إلى طابور السحوبات
      await withdrawalQueue.add('process-withdrawal', {
        requestId: withdrawalRequest.insertedId,
        userId: userId,
        amount: amount,
        walletAddress: wallet_address
      }, {
        attempts: 3,  // إعادة المحاولة 3 مرات إذا فشل
        backoff: {
          type: 'exponential',
          delay: 5000  // 5 ثوانٍ، ثم 10، ثم 20
        }
      });
    });
    
    await session.endSession();
    
    // 5. الرد للمستخدم
    res.json({
      success: true,
      message: 'تم إنشاء طلب السحب بنجاح. سيتم المعالجة خلال دقائق.'
    });
    
  } catch (error) {
    logger.error('Withdrawal request error:', error);
    res.status(500).json({
      success: false,
      error: 'حدث خطأ. يرجى المحاولة لاحقاً'
    });
  }
});
```

**الخطوة 4: معالج الطابور يعالج السحب**
```javascript
// في ملف withdrawal-queue.js

async function processWithdrawal(job) {
  const { requestId, userId, amount, walletAddress } = job.data;
  
  try {
    logger.info(`🔄 Processing withdrawal ${requestId} for user ${userId}`);
    
    // 1. تحديث الحالة إلى "processing"
    await db.collection('withdrawal_requests').updateOne(
      { _id: requestId },
      { $set: { status: 'processing' } }
    );
    
    // 2. إرسال السحب عبر OKX API
    const okxService = require('./okx');
    const withdrawalResult = await okxService.withdraw({
      coin: 'USDT',
      amount: amount,
      toAddress: walletAddress,
      chain: 'USDT-TRC20',
      fee: 1
    });
    
    if (!withdrawalResult.success) {
      throw new Error(withdrawalResult.error);
    }
    
    // 3. تحديث حالة الطلب إلى "completed"
    await db.collection('withdrawal_requests').updateOne(
      { _id: requestId },
      {
        $set: {
          status: 'completed',
          withdraw_id: withdrawalResult.data.withdrawId,
          transaction_hash: withdrawalResult.data.txid,
          processed_at: new Date()
        }
      }
    );
    
    // 4. تحديث حالة المعاملة
    await db.collection('transactions').updateOne(
      { user_id: userId, type: 'withdrawal', status: 'pending' },
      {
        $set: {
          status: 'completed',
          transaction_hash: withdrawalResult.data.txid
        }
      }
    );
    
    // 5. إشعار المستخدم
    await bot.sendMessage(userId, `
✅ <b>تم السحب بنجاح!</b>

💰 <b>المبلغ:</b> ${amount} USDT
📍 <b>العنوان:</b> <code>${walletAddress}</code>
🔗 <b>المعاملة:</b> <code>${withdrawalResult.data.txid}</code>

⏰ ستصل الأموال خلال 5-15 دقيقة
    `, { parse_mode: 'HTML' });
    
    // 6. إشعار المالك
    await bot.sendMessage(OWNER_ID, `
✅ <b>سحب ناجح!</b>

👤 المستخدم: ${userId}
💰 المبلغ: ${amount} USDT
📍 العنوان: ${walletAddress}
🔗 Withdraw ID: ${withdrawalResult.data.withdrawId}
    `, { parse_mode: 'HTML' });
    
    logger.info(`✅ Withdrawal ${requestId} completed successfully`);
    
  } catch (error) {
    logger.error(`❌ Withdrawal ${requestId} failed:`, error);
    
    // إذا كانت هذه المحاولة الأخيرة
    if (job.attemptsMade >= job.opts.attempts - 1) {
      // إشعار المالك بالفشل النهائي
      await bot.sendMessage(OWNER_ID, `
🚨 <b>فشل سحب - يحتاج تدخل يدوي!</b>

📋 <b>معلومات الطلب:</b>
• Request ID: ${requestId}
• المستخدم: ${userId}
• المبلغ: ${amount} USDT
• العنوان: ${walletAddress}

⚠️ <b>السبب:</b>
${error.message}

🔧 <b>الإجراء المطلوب:</b>
قم بمعالجة السحب يدوياً من لوحة التحكم
      `, { parse_mode: 'HTML' });
      
      // تحديث الحالة إلى "failed"
      await db.collection('withdrawal_requests').updateOne(
        { _id: requestId },
        {
          $set: {
            status: 'failed',
            failure_reason: error.message,
            retry_count: job.attemptsMade + 1
          }
        }
      );
    }
    
    throw error;  // لإعادة المحاولة
  }
}

// تشغيل معالج الطابور
withdrawalQueue.process('process-withdrawal', 5, processWithdrawal);
```

#### 5.3.2 الأمان في نظام السحب

**1. التحققات المتعددة**
- التحقق من الرصيد
- التحقق من عنوان المحفظة
- التحقق من عدم وجود طلبات معلقة
- التحقق من الحدود اليومية

**2. معاملات MongoDB**
- خصم المبلغ والسحب في عملية واحدة
- إذا فشل السحب، يُعاد المبلغ تلقائياً

**3. نظام إعادة المحاولة**
- إذا فشل السحب، يُعاد تلقائياً
- حتى 3 محاولات
- تأخير متزايد بين المحاولات (5 ثوانٍ، 10، 20)

**4. إشعارات المالك**
- المالك يستلم إشعار عن كل سحب
- إذا فشل السحب نهائياً، المالك يستلم تنبيه فوري

**5. منع السحب المزدوج**
- كل طلب سحب له معرف فريد
- لا يمكن معالجة نفس الطلب مرتين
- نتحقق من الحالة قبل المعالجة

### 5.4 إحصائيات النظام المالي

يمكن للمالك رؤية إحصائيات شاملة من لوحة التحكم:

```javascript
// إجمالي الإيداعات
const totalDeposits = await db.collection('transactions')
  .aggregate([
    { $match: { type: 'deposit', status: 'completed' } },
    { $group: { _id: null, total: { $sum: '$amount' } } }
  ]).toArray();

// إجمالي السحوبات
const totalWithdrawals = await db.collection('transactions')
  .aggregate([
    { $match: { type: 'withdrawal', status: 'completed' } },
    { $group: { _id: null, total: { $sum: '$amount' } } }
  ]).toArray();

// الأرباح (من رسوم الاشتراكات والسحوبات)
const totalRevenue = await db.collection('transactions')
  .aggregate([
    {
      $match: {
        type: { $in: ['subscription', 'withdrawal'] },
        status: 'completed'
      }
    },
    { $group: { _id: null, total: { $sum: '$amount' } } }
  ]).toArray();

// عرض في لوحة التحكم
console.log(`
📊 الإحصائيات المالية:

💰 إجمالي الإيداعات: ${totalDeposits[0].total} USDT
💸 إجمالي السحوبات: ${totalWithdrawals[0].total} USDT
💵 الأرباح: ${totalRevenue[0].total} USDT
📈 الصافي: ${totalDeposits[0].total - totalWithdrawals[0].total} USDT
`);
```

---

## 6. أنواع التحليلات الفنية

### 6.1 نظرة عامة

المشروع يقدم **5 أنواع مختلفة** من التحليلات الفنية، كل واحد له مستوى مختلف من الدقة والتعقيد:

1. **Regular Analysis** (التحليل العادي) - الأساسي والسريع
2. **Ultra Analysis** (التحليل الفائق) - عالي الدقة
3. **Zero Reversal Analysis** (التحليل الصفري الانعكاس) - دقة قصوى
4. **V1 PRO AI Analysis** (التحليل الاحترافي بالذكاء الاصطناعي) - مدعوم بالـ AI
5. **Master Analysis** (التحليل الرئيسي) - الأشمل والأقوى

### 6.2 التحليل العادي (Regular Analysis)

#### 6.2.1 الوصف
هذا هو التحليل **الأساسي والسريع**. مناسب للمبتدئين والاستخدام اليومي.

#### 6.2.2 المؤشرات المستخدمة (20+ مؤشر)

**1. المتوسطات المتحركة (Moving Averages)**:
- EMA 20: متوسط متحرك أسي قصير المدى
- EMA 50: متوسط متحرك أسي متوسط المدى
- EMA 200: متوسط متحرك أسي طويل المدى
- SMA 20/50/200: متوسطات متحركة بسيطة

**2. مؤشرات الزخم (Momentum Indicators)**:
- **RSI (Relative Strength Index)**: قوة الاتجاه
  - < 30: منطقة تشبع بيعي (oversold) → فرصة شراء
  - > 70: منطقة تشبع شرائي (overbought) → فرصة بيع
  - 40-60: منطقة محايدة
  
- **MACD (Moving Average Convergence Divergence)**:
  - خط MACD فوق خط الإشارة: إشارة شراء
  - خط MACD تحت خط الإشارة: إشارة بيع
  
- **Stochastic**: مؤشر عشوائي
  - < 20: تشبع بيعي
  - > 80: تشبع شرائي

**3. مؤشرات التقلب (Volatility Indicators)**:
- **Bollinger Bands**: نطاقات بولينجر
  - السعر قرب الحد العلوي: تشبع شرائي محتمل
  - السعر قرب الحد السفلي: تشبع بيعي محتمل
  - السعر في المنتصف: محايد
  
- **ATR (Average True Range)**: متوسط المدى الحقيقي
  - قيمة عالية: تقلب عالي
  - قيمة منخفضة: تقلب منخفض

**4. مؤشرات القوة (Strength Indicators)**:
- **ADX (Average Directional Index)**: قوة الاتجاه
  - < 20: اتجاه ضعيف (السوق عرضي)
  - 20-40: اتجاه معتدل
  - > 40: اتجاه قوي جداً
  
- **Volume**: حجم التداول
  - حجم عالي + سعر صاعد: اتجاه صاعد قوي
  - حجم عالي + سعر هابط: اتجاه هابط قوي
  - حجم منخفض: ضعف في الاتجاه

**5. مؤشرات أخرى**:
- **CCI (Commodity Channel Index)**
- **Williams %R**
- **ROC (Rate of Change)**
- **OBV (On-Balance Volume)**

#### 6.2.3 آلية العمل

```javascript
// في ملف analysis.js

async function completeAnalysis(symbol, timeframe, marketType, lang = 'ar') {
  // 1. جلب بيانات الشموع (Candles)
  const candles = await getCandles(symbol, timeframe, marketType);
  
  if (!candles || candles.length < 200) {
    return { success: false, error: 'بيانات غير كافية للتحليل' };
  }
  
  // 2. استخراج بيانات السعر والحجم
  const closePrices = candles.map(c => c.close);
  const highPrices = candles.map(c => c.high);
  const lowPrices = candles.map(c => c.low);
  const volumes = candles.map(c => c.volume);
  
  // 3. حساب المؤشرات
  const rsi = technicalIndicators.RSI.calculate({
    values: closePrices,
    period: 14
  });
  
  const macd = technicalIndicators.MACD.calculate({
    values: closePrices,
    fastPeriod: 12,
    slowPeriod: 26,
    signalPeriod: 9,
    SimpleMAOscillator: false,
    SimpleMASignal: false
  });
  
  const bb = technicalIndicators.BollingerBands.calculate({
    values: closePrices,
    period: 20,
    stdDev: 2
  });
  
  const ema20 = technicalIndicators.EMA.calculate({
    values: closePrices,
    period: 20
  });
  
  const ema50 = technicalIndicators.EMA.calculate({
    values: closePrices,
    period: 50
  });
  
  const ema200 = technicalIndicators.EMA.calculate({
    values: closePrices,
    period: 200
  });
  
  const stochastic = technicalIndicators.Stochastic.calculate({
    high: highPrices,
    low: lowPrices,
    close: closePrices,
    period: 14,
    signalPeriod: 3
  });
  
  const adx = technicalIndicators.ADX.calculate({
    high: highPrices,
    low: lowPrices,
    close: closePrices,
    period: 14
  });
  
  // 4. الحصول على آخر قيمة من كل مؤشر
  const currentRSI = rsi[rsi.length - 1];
  const currentMACD = macd[macd.length - 1];
  const currentBB = bb[bb.length - 1];
  const currentEMA20 = ema20[ema20.length - 1];
  const currentEMA50 = ema50[ema50.length - 1];
  const currentEMA200 = ema200[ema200.length - 1];
  const currentStoch = stochastic[stochastic.length - 1];
  const currentADX = adx[adx.length - 1];
  const currentPrice = closePrices[closePrices.length - 1];
  
  // 5. تحليل كل مؤشر
  let buySignals = 0;
  let sellSignals = 0;
  let neutralSignals = 0;
  const signals = [];
  
  // تحليل RSI
  if (currentRSI < 30) {
    buySignals++;
    signals.push({
      indicator: 'RSI',
      value: currentRSI,
      signal: 'buy',
      reason: 'تشبع بيعي - فرصة شراء'
    });
  } else if (currentRSI > 70) {
    sellSignals++;
    signals.push({
      indicator: 'RSI',
      value: currentRSI,
      signal: 'sell',
      reason: 'تشبع شرائي - فرصة بيع'
    });
  } else {
    neutralSignals++;
    signals.push({
      indicator: 'RSI',
      value: currentRSI,
      signal: 'neutral',
      reason: 'منطقة محايدة'
    });
  }
  
  // تحليل MACD
  if (currentMACD.MACD > currentMACD.signal) {
    buySignals++;
    signals.push({
      indicator: 'MACD',
      signal: 'buy',
      reason: 'خط MACD فوق خط الإشارة'
    });
  } else {
    sellSignals++;
    signals.push({
      indicator: 'MACD',
      signal: 'sell',
      reason: 'خط MACD تحت خط الإشارة'
    });
  }
  
  // تحليل المتوسطات المتحركة
  if (currentPrice > currentEMA20 && currentEMA20 > currentEMA50 && currentEMA50 > currentEMA200) {
    buySignals++;
    signals.push({
      indicator: 'EMA Alignment',
      signal: 'buy',
      reason: 'اتجاه صاعد قوي - المتوسطات متراصة'
    });
  } else if (currentPrice < currentEMA20 && currentEMA20 < currentEMA50 && currentEMA50 < currentEMA200) {
    sellSignals++;
    signals.push({
      indicator: 'EMA Alignment',
      signal: 'sell',
      reason: 'اتجاه هابط قوي - المتوسطات متراصة'
    });
  }
  
  // تحليل Bollinger Bands
  if (currentPrice < currentBB.lower) {
    buySignals++;
    signals.push({
      indicator: 'Bollinger Bands',
      signal: 'buy',
      reason: 'السعر تحت الحد السفلي - فرصة شراء'
    });
  } else if (currentPrice > currentBB.upper) {
    sellSignals++;
    signals.push({
      indicator: 'Bollinger Bands',
      signal: 'sell',
      reason: 'السعر فوق الحد العلوي - فرصة بيع'
    });
  }
  
  // تحليل ADX
  let trendStrength = 'ضعيف';
  if (currentADX.adx > 40) {
    trendStrength = 'قوي جداً';
  } else if (currentADX.adx > 25) {
    trendStrength = 'متوسط';
  }
  
  signals.push({
    indicator: 'ADX',
    value: currentADX.adx,
    signal: 'info',
    reason: `قوة الاتجاه: ${trendStrength}`
  });
  
  // ... المزيد من التحليلات لباقي المؤشرات
  
  // 6. حساب التوصية النهائية
  const totalSignals = buySignals + sellSignals + neutralSignals;
  const buyPercentage = (buySignals / totalSignals) * 100;
  const sellPercentage = (sellSignals / totalSignals) * 100;
  
  let recommendation = 'انتظار';
  let confidence = 0;
  
  if (buyPercentage > 60) {
    recommendation = 'شراء قوي';
    confidence = buyPercentage;
  } else if (buyPercentage > 50) {
    recommendation = 'شراء';
    confidence = buyPercentage;
  } else if (sellPercentage > 60) {
    recommendation = 'بيع قوي';
    confidence = sellPercentage;
  } else if (sellPercentage > 50) {
    recommendation = 'بيع';
    confidence = sellPercentage;
  } else {
    recommendation = 'انتظار';
    confidence = 50;
  }
  
  // 7. حساب نقاط الدخول والخروج
  const support = calculateSupport(candles);  // مستوى الدعم
  const resistance = calculateResistance(candles);  // مستوى المقاومة
  
  const entryPoints = [];
  const exitPoints = [];
  const stopLoss = null;
  
  if (recommendation.includes('شراء')) {
    entryPoints.push({
      price: support,
      type: 'limit',
      description: 'شراء عند مستوى الدعم'
    });
    entryPoints.push({
      price: currentPrice,
      type: 'market',
      description: 'شراء فوري بسعر السوق'
    });
    
    exitPoints.push({
      price: resistance,
      type: 'limit',
      description: 'بيع عند مستوى المقاومة'
    });
    
    stopLoss = {
      price: support * 0.95,  // 5% تحت الدعم
      description: 'وقف الخسارة'
    };
  } else if (recommendation.includes('بيع')) {
    entryPoints.push({
      price: resistance,
      type: 'limit',
      description: 'بيع عند مستوى المقاومة'
    });
    entryPoints.push({
      price: currentPrice,
      type: 'market',
      description: 'بيع فوري بسعر السوق'
    });
    
    exitPoints.push({
      price: support,
      type: 'limit',
      description: 'إعادة الشراء عند مستوى الدعم'
    });
    
    stopLoss = {
      price: resistance * 1.05,  // 5% فوق المقاومة
      description: 'وقف الخسارة'
    };
  }
  
  // 8. إرجاع النتيجة الكاملة
  return {
    success: true,
    analysis: {
      symbol: symbol,
      timeframe: timeframe,
      marketType: marketType,
      currentPrice: currentPrice,
      
      recommendation: recommendation,
      confidence: Math.round(confidence),
      trendStrength: trendStrength,
      
      buySignals: buySignals,
      sellSignals: sellSignals,
      neutralSignals: neutralSignals,
      
      signals: signals,
      
      entryPoints: entryPoints,
      exitPoints: exitPoints,
      stopLoss: stopLoss,
      
      support: support,
      resistance: resistance,
      
      indicators: {
        rsi: currentRSI,
        macd: currentMACD,
        ema20: currentEMA20,
        ema50: currentEMA50,
        ema200: currentEMA200,
        bollingerBands: currentBB,
        stochastic: currentStoch,
        adx: currentADX
      },
      
      timestamp: new Date()
    }
  };
}
```

#### 6.2.4 مثال على النتيجة

```json
{
  "success": true,
  "analysis": {
    "symbol": "BTCUSDT",
    "timeframe": "1h",
    "marketType": "crypto",
    "currentPrice": 50000,
    
    "recommendation": "شراء قوي",
    "confidence": 75,
    "trendStrength": "قوي جداً",
    
    "buySignals": 15,
    "sellSignals": 3,
    "neutralSignals": 2,
    
    "signals": [
      {
        "indicator": "RSI",
        "value": 28,
        "signal": "buy",
        "reason": "تشبع بيعي - فرصة شراء"
      },
      {
        "indicator": "MACD",
        "signal": "buy",
        "reason": "خط MACD فوق خط الإشارة"
      },
      ...
    ],
    
    "entryPoints": [
      {
        "price": 49500,
        "type": "limit",
        "description": "شراء عند مستوى الدعم"
      },
      {
        "price": 50000,
        "type": "market",
        "description": "شراء فوري بسعر السوق"
      }
    ],
    
    "exitPoints": [
      {
        "price": 52000,
        "type": "limit",
        "description": "بيع عند مستوى المقاومة"
      }
    ],
    
    "stopLoss": {
      "price": 47025,
      "description": "وقف الخسارة"
    },
    
    "support": 49500,
    "resistance": 52000,
    
    "indicators": {
      "rsi": 28,
      "macd": {...},
      "ema20": 49800,
      "ema50": 49000,
      "ema200": 48000,
      ...
    },
    
    "timestamp": "2025-01-15T10:30:00.000Z"
  }
}
```

### 6.3 التحليل الفائق (Ultra Analysis)

#### 6.3.1 الوصف
تحليل **عالي الدقة** يتطلب توافق قوي بين المؤشرات. مناسب للمتداولين المحترفين الذين يريدون إشارات قليلة لكن عالية الجودة.

#### 6.3.2 المعايير الصارمة

```javascript
// في ملف ultra-analysis.js

const ULTRA_CRITERIA = {
  minIndicatorAgreement: 0.75,  // 75% من المؤشرات يجب أن تتفق
  minADX: 25,                   // قوة اتجاه لا تقل عن 25
  minConfirmations: 6,          // 6 تأكيدات على الأقل
  minVolume: 1.5,               // حجم تداول أعلى من المتوسط بـ 50%
  riskRewardRatio: 1.5          // نسبة ربح/خسارة لا تقل عن 1.5:1
};

async function ultraAnalysis(symbol, timeframe, marketType, lang = 'ar') {
  // 1. جلب التحليل العادي أولاً
  const basicAnalysis = await completeAnalysis(symbol, timeframe, marketType, lang);
  
  if (!basicAnalysis.success) {
    return basicAnalysis;
  }
  
  const analysis = basicAnalysis.analysis;
  
  // 2. التحقق من المعايير الصارمة
  const totalSignals = analysis.buySignals + analysis.sellSignals + analysis.neutralSignals;
  const strongestSignalCount = Math.max(analysis.buySignals, analysis.sellSignals);
  const agreementPercentage = strongestSignalCount / totalSignals;
  
  // معيار 1: توافق 75%+
  if (agreementPercentage < ULTRA_CRITERIA.minIndicatorAgreement) {
    return {
      success: false,
      error: `توافق ضعيف بين المؤشرات (${(agreementPercentage*100).toFixed(0)}%). يتطلب 75%+`,
      recommendation: 'انتظار'
    };
  }
  
  // معيار 2: ADX قوي
  if (analysis.indicators.adx.adx < ULTRA_CRITERIA.minADX) {
    return {
      success: false,
      error: `قوة الاتجاه ضعيفة (ADX ${analysis.indicators.adx.adx.toFixed(0)}). يتطلب 25+`,
      recommendation: 'انتظار'
    };
  }
  
  // معيار 3: عدد التأكيدات
  if (strongestSignalCount < ULTRA_CRITERIA.minConfirmations) {
    return {
      success: false,
      error: `تأكيدات قليلة (${strongestSignalCount}). يتطلب 6+`,
      recommendation: 'انتظار'
    };
  }
  
  // معيار 4: حجم التداول
  const avgVolume = calculateAverageVolume(candles, 20);
  const currentVolume = candles[candles.length - 1].volume;
  const volumeRatio = currentVolume / avgVolume;
  
  if (volumeRatio < ULTRA_CRITERIA.minVolume) {
    return {
      success: false,
      error: `حجم تداول منخفض (${volumeRatio.toFixed(2)}x). يتطلب 1.5x+`,
      recommendation: 'انتظار'
    };
  }
  
  // معيار 5: نسبة ربح/خسارة
  const potentialProfit = Math.abs(analysis.exitPoints[0].price - analysis.entryPoints[0].price);
  const potentialLoss = Math.abs(analysis.entryPoints[0].price - analysis.stopLoss.price);
  const riskRewardRatio = potentialProfit / potentialLoss;
  
  if (riskRewardRatio < ULTRA_CRITERIA.riskRewardRatio) {
    return {
      success: false,
      error: `نسبة ربح/خسارة ضعيفة (${riskRewardRatio.toFixed(2)}:1). يتطلب 1.5:1+`,
      recommendation: 'انتظار'
    };
  }
  
  // ✅ جميع المعايير متحققة!
  return {
    success: true,
    analysis: {
      ...analysis,
      type: 'ultra',
      quality: 'عالي جداً',
      agreementPercentage: agreementPercentage * 100,
      volumeRatio: volumeRatio,
      riskRewardRatio: riskRewardRatio,
      ultraCriteriaMet: {
        indicatorAgreement: true,
        adxStrength: true,
        confirmations: true,
        volume: true,
        riskReward: true
      }
    }
  };
}
```

#### 6.3.3 الفرق بين Regular و Ultra

| المعيار | Regular Analysis | Ultra Analysis |
|---------|------------------|----------------|
| توافق المؤشرات | 50%+ | 75%+ |
| ADX (قوة الاتجاه) | أي قيمة | 25+ |
| عدد التأكيدات | أي عدد | 6+ |
| حجم التداول | عادي | 1.5x المتوسط |
| نسبة ربح/خسارة | أي نسبة | 1.5:1+ |
| عدد الإشارات | كثيرة | قليلة لكن دقيقة |
| الدقة المتوقعة | 60-70% | 80-90% |

### 6.4 تحليل صفر الانعكاس (Zero Reversal Analysis)

#### 6.4.1 الوصف
أقوى وأدق تحليل في المشروع. يهدف إلى **0% احتمال انعكاس**. نادراً ما يعطي إشارات، لكن عندما يعطي، تكون شبه مؤكدة.

#### 6.4.2 المعايير الصارمة جداً

```javascript
// في ملف zero-reversal-analysis.js

const ZERO_REVERSAL_CRITERIA = {
  minScore: 38,           // من أصل 41 نقطة (93%+)
  minADX: 45,            // اتجاه قوي جداً
  riskRewardRatio: 4,    // نسبة ربح/خسارة 4:1
  minVolume: 3,          // حجم تداول 3x المتوسط
  trendClearness: 1.0    // اتجاه واضح 100%
};

async function zeroReversalAnalysis(symbol, timeframe, marketType, lang = 'ar') {
  // نظام نقاط شامل
  let score = 0;
  const maxScore = 41;
  const reasons = [];
  
  // ... جلب البيانات والمؤشرات
  
  // 1. تقييم RSI (5 نقاط)
  if (currentRSI < 20) {
    score += 5;
    reasons.push('RSI في منطقة تشبع بيعي حاد (<20)');
  } else if (currentRSI > 80) {
    score += 5;
    reasons.push('RSI في منطقة تشبع شرائي حاد (>80)');
  }
  
  // 2. تقييم MACD (5 نقاط)
  const macdHistogram = currentMACD.histogram;
  if (Math.abs(macdHistogram) > 2) {  // فجوة كبيرة
    score += 5;
    reasons.push('MACD يظهر فجوة كبيرة');
  }
  
  // 3. تقييم ADX (8 نقاط)
  if (currentADX.adx >= 50) {
    score += 8;
    reasons.push(`ADX قوي جداً (${currentADX.adx.toFixed(0)})`);
  } else if (currentADX.adx >= 45) {
    score += 5;
    reasons.push(`ADX قوي (${currentADX.adx.toFixed(0)})`);
  }
  
  // 4. تقييم المتوسطات المتحركة (6 نقاط)
  const emaAlignment = checkEMAAlignment(currentPrice, currentEMA20, currentEMA50, currentEMA200);
  if (emaAlignment === 'perfect_bullish' || emaAlignment === 'perfect_bearish') {
    score += 6;
    reasons.push('المتوسطات المتحركة متراصة بشكل مثالي');
  }
  
  // 5. تقييم حجم التداول (5 نقاط)
  if (volumeRatio >= 3) {
    score += 5;
    reasons.push(`حجم تداول ضخم (${volumeRatio.toFixed(1)}x)`);
  }
  
  // 6. تقييم Bollinger Bands (4 نقاط)
  const bbWidth = (currentBB.upper - currentBB.lower) / currentBB.middle;
  if (bbWidth > 0.05) {  // اتساع كبير
    score += 4;
    reasons.push('Bollinger Bands متسعة - تقلب عالي');
  }
  
  // 7. تقييم Stochastic (3 نقاط)
  if (currentStoch.k < 10 || currentStoch.k > 90) {
    score += 3;
    reasons.push(`Stochastic في منطقة متطرفة (${currentStoch.k.toFixed(0)})`);
  }
  
  // 8. تقييم نسبة ربح/خسارة (5 نقاط)
  if (riskRewardRatio >= 4) {
    score += 5;
    reasons.push(`نسبة ربح/خسارة ممتازة (${riskRewardRatio.toFixed(1)}:1)`);
  }
  
  // التحقق من المعايير
  if (score < ZERO_REVERSAL_CRITERIA.minScore) {
    return {
      success: false,
      error: `النقاط غير كافية (${score}/${maxScore}). يتطلب ${ZERO_REVERSAL_CRITERIA.minScore}+`,
      score: score,
      maxScore: maxScore,
      recommendation: 'انتظار'
    };
  }
  
  if (currentADX.adx < ZERO_REVERSAL_CRITERIA.minADX) {
    return {
      success: false,
      error: `ADX ضعيف (${currentADX.adx.toFixed(0)}). يتطلب ${ZERO_REVERSAL_CRITERIA.minADX}+`,
      recommendation: 'انتظار'
    };
  }
  
  // ✅ جميع المعايير متحققة!
  return {
    success: true,
    analysis: {
      type: 'zero_reversal',
      quality: 'استثنائي',
      score: score,
      maxScore: maxScore,
      scorePercentage: (score / maxScore) * 100,
      reversalProbability: 0,  // 0%!
      recommendation: analysis.buySignals > analysis.sellSignals ? 'شراء فوري' : 'بيع فوري',
      confidence: 95,
      reasons: reasons,
      ...analysis
    }
  };
}
```

### 6.5 تحليل V1 PRO بالذكاء الاصطناعي

#### 6.5.1 الوصف
يجمع بين **التحليل الفني التقليدي** و**الذكاء الاصطناعي** من Google Gemini. يحلل:
- المؤشرات الفنية
- أخبار السوق
- مشاعر المتداولين (Sentiment Analysis)
- المخاطر المحتملة

#### 6.5.2 كيف يعمل؟

```javascript
// في ملف v1-pro-analysis.js

async function v1ProAnalysis(symbol, timeframe, marketType, lang = 'ar') {
  // 1. التحليل الفني الأساسي
  const basicAnalysis = await completeAnalysis(symbol, timeframe, marketType, lang);
  
  if (!basicAnalysis.success) {
    return basicAnalysis;
  }
  
  // 2. تحليل AI (إذا كان مفعلاً)
  let aiInsights = null;
  let sentimentAnalysis = null;
  let riskAssessment = null;
  
  if (config.AI_FEATURES_ENABLED) {
    const geminiService = require('./gemini-service');
    
    // طلب تحليل من Gemini AI
    const aiPrompt = `
أنت محلل مالي خبير. قم بتحليل ${symbol} على إطار ${timeframe}.

البيانات الفنية الحالية:
- السعر: ${basicAnalysis.analysis.currentPrice}
- RSI: ${basicAnalysis.analysis.indicators.rsi.toFixed(2)}
- MACD: ${JSON.stringify(basicAnalysis.analysis.indicators.macd)}
- قوة الاتجاه (ADX): ${basicAnalysis.analysis.indicators.adx.adx.toFixed(2)}
- التوصية الأولية: ${basicAnalysis.analysis.recommendation}

أعطني:
1. تحليل للمشاعر السوقية
2. تقييم للمخاطر (منخفض/متوسط/عالي)
3. توصية نهائية مع الأسباب
4. نقاط دخول وخروج محسّنة
`;
    
    const aiResponse = await geminiService.chat(aiPrompt);
    
    aiInsights = aiResponse.content;
    
    // استخراج تحليل المشاعر
    sentimentAnalysis = extractSentiment(aiInsights);  // إيجابي، سلبي، محايد
    
    // استخراج تقييم المخاطر
    riskAssessment = extractRiskLevel(aiInsights);  // منخفض، متوسط، عالي
  }
  
  // 3. دمج النتائج
  return {
    success: true,
    analysis: {
      ...basicAnalysis.analysis,
      type: 'v1_pro_ai',
      quality: 'احترافي مع AI',
      
      // AI Insights
      aiEnabled: config.AI_FEATURES_ENABLED,
      aiInsights: aiInsights,
      sentimentAnalysis: sentimentAnalysis,
      riskAssessment: riskAssessment,
      
      // توصية محسّنة
      enhancedRecommendation: combineRecommendations(
        basicAnalysis.analysis.recommendation,
        sentimentAnalysis,
        riskAssessment
      )
    }
  };
}

// دمج التوصيات
function combineRecommendations(technicalRec, sentiment, risk) {
  // إذا كان التحليل الفني يقول "شراء" والمشاعر إيجابية والمخاطر منخفضة
  if (technicalRec.includes('شراء') && sentiment === 'positive' && risk === 'low') {
    return {
      action: 'شراء فوري',
      confidence: 90,
      reason: 'تحليل فني قوي + مشاعر إيجابية + مخاطر منخفضة'
    };
  }
  
  // إذا كان التحليل الفني يقول "شراء" لكن المشاعر سلبية أو المخاطر عالية
  if (technicalRec.includes('شراء') && (sentiment === 'negative' || risk === 'high')) {
    return {
      action: 'انتظار',
      confidence: 40,
      reason: 'تحليل فني إيجابي لكن المشاعر/المخاطر سلبية - انتظر'
    };
  }
  
  // ... المزيد من السيناريوهات
}
```

### 6.6 التحليل الرئيسي (Master Analysis)

#### 6.6.1 الوصف
**الأشمل والأقوى**. يجمع بين:
- التحليل الفني على أطر زمنية متعددة
- تحليل الحجم المتقدم
- أنماط الشموع اليابانية
- الذكاء الاصطناعي
- تحليل المخاطر

```javascript
async function masterAnalysis(symbol, marketType, lang = 'ar') {
  // 1. تحليل متعدد الأطر الزمنية
  const timeframes = ['15m', '1h', '4h', '1d'];
  const analyses = {};
  
  for (const tf of timeframes) {
    analyses[tf] = await v1ProAnalysis(symbol, tf, marketType, lang);
  }
  
  // 2. التحقق من التوافق بين الأطر
  const recommendations = timeframes.map(tf => analyses[tf].analysis.recommendation);
  const consensus = findConsensus(recommendations);
  
  // 3. تحليل أنماط الشموع
  const candlePatterns = await analyzeCandlePatterns(symbol, marketType);
  
  // 4. تحليل عمق السوق
  const orderBookAnalysis = await analyzeOrderBook(symbol, marketType);
  
  // 5. دمج كل شيء
  return {
    success: true,
    analysis: {
      type: 'master',
      quality: 'شامل ومتقدم',
      
      multiTimeframeAnalysis: analyses,
      consensus: consensus,
      candlePatterns: candlePatterns,
      orderBookAnalysis: orderBookAnalysis,
      
      finalRecommendation: generateMasterRecommendation(
        consensus,
        candlePatterns,
        orderBookAnalysis
      )
    }
  };
}
```

---

**(ملاحظة: هذا جزء من الشرح الكامل. الملف كبير جداً ويحتوي على 20 قسماً. هل تريد أن أكمل باقي الأقسام؟)**

**الأقسام المتبقية:**
7. نظام الاشتراكات والمستويات
8. نظام المحللين والإشارات
9. نظام الإحالة والأرباح
10. نظام الأمان والحماية
11. الذكاء الاصطناعي AI
12. الأوامر المتاحة في البوت
13. تطبيق الويب Telegram Web App
14. مصادر البيانات APIs
15. التقنيات المستخدمة
16. نظام الطوابير Queues
17. نظام التخزين المؤقت Caching
18. نظام المراقبة Monitoring
19. الأنظمة المتقدمة
20. النشر والبيئة
