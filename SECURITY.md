# دليل الأمان - OBENTCHI Trading Bot

## 🔒 الإجراءات الأمنية المطبقة

### 1. إدارة المتغيرات الحساسة
جميع البيانات الحساسة يتم تخزينها في متغيرات البيئة (Environment Variables):

**المتغيرات المطلوبة:**
- `BOT_TOKEN` - رمز بوت Telegram
- `OWNER_ID` - معرف مالك البوت
- `CHANNEL_ID` - معرف القناة
- `MONGODB_USER` - مستخدم قاعدة البيانات
- `MONGODB_PASSWORD` - كلمة مرور قاعدة البيانات

**المتغيرات الاختيارية:**
- `CHANNEL_USERNAME` - اسم مستخدم القناة
- `BOT_WALLET_ADDRESS` - عنوان محفظة البوت
- API Keys للخدمات الخارجية (OKX, CoinGecko, etc.)

⚠️ **تحذير:** لا تقم أبداً بكتابة هذه القيم مباشرة في الكود أو رفعها على Git.

### 2. حماية من XSS و Injection
- دالة `sanitizeInput()` محسّنة لتنظيف المدخلات (بما في ذلك التنظيف العميق للكائنات)
- دالة `sanitizeHTML()` تستخدم **DOMPurify** (مكتبة آمنة ومثبتة) لتنظيف محتوى HTML
  - حماية من unicode-escaped و URL-encoded payloads
  - قائمة محددة للـ tags و attributes المسموحة فقط
  - منع جميع event handlers و JavaScript URIs
- دالة `containsDangerousPatterns()` للكشف عن أنماط خطرة
- استعلامات قاعدة البيانات آمنة باستخدام parameterized queries

### 3. رؤوس الأمان (Security Headers)
التطبيق يستخدم رؤوس أمان متقدمة:

```
X-Content-Type-Options: nosniff
X-Frame-Options: DENY
X-XSS-Protection: 1; mode=block
Strict-Transport-Security: max-age=31536000; includeSubDomains
Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: geolocation=(), microphone=(), camera=()
```

### 4. Content Security Policy (CSP)
سياسة أمان محتوى صارمة:
- `default-src 'self'` - السماح فقط بالموارد من نفس المصدر
- `object-src 'none'` - منع تحميل plugins
- `base-uri 'self'` - منع تغيير base URL
- `form-action 'self'` - منع إرسال forms لمواقع خارجية
- `frame-ancestors 'none'` - منع embedding في iframes
- `upgrade-insecure-requests` - تحويل HTTP إلى HTTPS تلقائياً

### 5. تحديد معدل الطلبات (Rate Limiting)
- حد أقصى 60 طلب في الدقيقة لكل مستخدم
- تنظيف تلقائي للذاكرة كل 5 دقائق
- رسائل خطأ واضحة عند تجاوز الحد

### 6. التحقق من الهوية (Authentication)
- التحقق من بيانات Telegram WebApp باستخدام HMAC-SHA256
- التحقق من صحة التوقيع لكل طلب API
- استخراج آمن لمعرف المستخدم من البيانات المتحققة

### 7. السجلات الآمنة (Secure Logging)
- عدم تسجيل بيانات حساسة (passwords, API keys, tokens)
- تسجيل الأخطاء برسائل آمنة فقط
- إرسال تنبيهات للمالك عند الأنشطة المشبوهة

### 8. التحقق من صحة المدخلات
- التحقق من صحة عناوين TRON
- التحقق من صحة معرفات المعاملات
- فلترة المحتوى المحظور (روابط، معرفات Telegram، كلمات محظورة)
- حد أقصى لأحجام الطلبات (10MB)

## 🔐 أفضل الممارسات

### للمطورين:
1. **لا تكتب أسراراً في الكود** - استخدم متغيرات البيئة دائماً
2. **استخدم دوال التنظيف** - `sanitizeInput()` و `sanitizeHTML()` عند التعامل مع مدخلات المستخدم
3. **تحقق من الأنماط الخطرة** - استخدم `containsDangerousPatterns()` قبل معالجة البيانات
4. **سجل بحذر** - لا تسجل بيانات حساسة في console.log
5. **استخدم HTTPS** - دائماً في الإنتاج

### للنشر (Deployment):
1. تأكد من تعيين جميع المتغيرات المطلوبة في `.env`
2. راجع ملف `.env.example` لمعرفة المتغيرات المطلوبة
3. تأكد من تفعيل HTTPS على الخادم
4. قم بمراجعة السجلات بانتظام للكشف عن أي نشاط مشبوه
5. حدّث التبعيات بانتظام: `npm update`

## 🚨 الإبلاغ عن الثغرات الأمنية

إذا اكتشفت ثغرة أمنية، يرجى:
1. عدم الإعلان عنها علناً
2. التواصل مع مالك البوت مباشرة عبر Telegram
3. تقديم معلومات تفصيلية عن الثغرة وكيفية استغلالها
4. إعطاء وقت معقول للإصلاح قبل الإفصاح

## 📋 قائمة التحقق الأمنية

- [x] المتغيرات الحساسة في Environment Variables
- [x] تنظيف وتحقق من المدخلات
- [x] رؤوس الأمان مفعلة
- [x] Content Security Policy محسنة
- [x] Rate Limiting مفعل
- [x] التحقق من Telegram WebApp Data
- [x] السجلات آمنة وخالية من البيانات الحساسة
- [x] HTTPS مفعل (في الإنتاج)
- [x] .gitignore يمنع رفع الأسرار
- [x] حماية من XSS و SQL/NoSQL Injection

## 🔄 التحديثات الأمنية

### الإصدار الحالي
- تاريخ آخر تحديث أمني: 2025-10-09
- تحسينات CSP وإضافة security headers
- تنظيف شامل للسجلات
- تحسين دوال التنظيف والفلترة

---

**ملاحظة:** الأمان مسؤولية مستمرة. راجع هذا الدليل وحدّث الإجراءات بانتظام.
